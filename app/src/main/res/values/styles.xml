<resources>

    <!-- Base application theme. -->
    <style name="Theme.LasteMV" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/toolbar</item>
        <item name="android:windowBackground">@color/primary</item>

    </style>

    <style name="TypeFacedText" parent="@android:style/TextAppearance.Medium">
        <item name="font">Cantarell-Regular.ttf</item>
    </style>

    <style name="FontBold">
        <item name="font">Cantarell-Bold.ttf</item>
    </style>


    <style name="Button.White.Bold" parent="FontBold">
        <item name="android:background">@drawable/btn_normal_rounded</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:minHeight">@dimen/main_fragment_component_height</item>
        <item name="android:minWidth">@dimen/main_fragment_normal_btn_width</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">@dimen/text_size_common_1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/primary</item>
    </style>

    <style name="Button.Orange.Bold" parent="FontBold">
        <item name="android:background">@drawable/btn_accent_rounded</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:minHeight">@dimen/main_fragment_component_height</item>
        <item name="android:minWidth">@dimen/main_fragment_normal_btn_width</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">@dimen/text_size_common_1</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="Button.White.Long.Bold" parent="Button.White.Bold">
        <item name="android:minWidth">@dimen/main_fragment_long_btn_width</item>
    </style>




    <style name="toolbar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">?attr/actionBarSize</item>
        <item name="android:background">@color/primary</item>
        <!-- android:textColorPrimary is the  color of the title text
       in the Toolbar, in the Theme.AppCompat theme:  -->
        <item name="android:textColorPrimary">@color/white</item>

        <!-- android:textColorPrimaryInverse is the  color of the title
       text in the Toolbar, in the Theme.AppCompat.Light theme:  -->
        <item name="android:textColorPrimaryInverse">@color/white</item>
        <!-- android:actionMenuTextColor is the color of the text of
        action (menu) items in the Toolbar, at least in the
        Theme.AppCompat theme.
        For some reason, they already get the textColorPrimary
        when running on API 21, but not on older versions of
        Android, so this is only necessary to support older
        Android versions.-->
        <item name="actionMenuTextColor">@color/white</item>
        <!-- android:textColorSecondary is the color of the menu
       overflow icon (three vertical dots) -->
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
